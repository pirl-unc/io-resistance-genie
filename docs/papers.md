# Papers

This page will list every paper seen by the pipeline, with metadata (title, authors, journal, date, DOI/PMID) and inclusion status from the triage step. It populates from `data/papers.jsonl` after the first scheduled run.

!!! note "Not yet populated"
    The pipeline skeleton is still being built. Return after the first scheduled literature ingest.
